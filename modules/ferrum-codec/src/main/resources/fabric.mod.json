{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "Ferrum region LZ4 block-stream native fast path.",
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
    "main": [
      "top.likoslupus.ferrum.codec.fabric.FerrumCodecFabric"
    ]
  },
  "mixins": [
    "ferrum-codec.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.16.0",
    "minecraft": "${minecraft}",
    "java": ">=25"
  }
}
