modLoader = "javafml"
loaderVersion = "[1,)"
license = "MIT"

[[mods]]
modId = "${id}"
version = "${version}"
displayName = "${name}"
description = "Ferrum region LZ4 block-stream native fast path."

[[mixins]]
config = "ferrum-codec.mixins.json"
