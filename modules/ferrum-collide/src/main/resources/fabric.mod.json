{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "Ferrum batch AABB clip and voxel-shape sweep native fast paths.",
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
    "main": [
      "top.likoslupus.ferrum.collide.fabric.FerrumCollideFabric"
    ]
  },
  "mixins": [
    "ferrum-collide.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.16.0",
    "minecraft": "${minecraft}",
    "java": ">=25"
  }
}
